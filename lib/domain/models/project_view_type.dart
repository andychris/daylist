/// How a project's tasks are laid out. Enum member names are persisted in
/// the database (see ProjectViewTypeConverter); do not rename existing
/// values without a migration.
enum ProjectViewType { list, board }
